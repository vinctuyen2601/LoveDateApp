# ====================
# API CONFIGURATION
# ====================
# ⚠️ QUAN TRỌNG: Thay YOUR_COMPUTER_IP bằng IP thật của máy tính
# Windows: Chạy lệnh "ipconfig" và tìm "IPv4 Address"
# Mac/Linux: Chạy lệnh "ifconfig" hoặc "hostname -I"
#
# Ví dụ:
# - Nếu IP là 192.168.1.50 → http://192.168.1.50:3000/api
# - Nếu IP là 10.0.0.25 → http://10.0.0.25:3000/api
#
# ❌ KHÔNG dùng: localhost, 127.0.0.1 (sẽ không hoạt động trên điện thoại)
# ✅ PHẢI dùng: IP thật của máy tính trên cùng mạng WiFi

REACT_APP_API_DEV_URL=http://YOUR_COMPUTER_IP:3000/api
REACT_APP_API_PROD_URL=https://your-production-api.com/api
REACT_APP_API_TIMEOUT=30000

# ====================
# APP CONFIGURATION
# ====================
REACT_APP_ENV=development
REACT_APP_VERSION=1.0.0
REACT_APP_DEBUG=true

# ====================
# HƯỚNG DẪN CÀI ĐẶT
# ====================
# 1. Copy file này thành .env:
#    cp .env.example .env    # Mac/Linux
#    copy .env.example .env  # Windows
#
# 2. Tìm IP máy tính:
#    Windows: ipconfig
#    Mac/Linux: ifconfig hoặc hostname -I
#
# 3. Cập nhật REACT_APP_API_DEV_URL với IP thật
#    Ví dụ: http://192.168.1.100:3000/api
#
# 4. Đảm bảo máy tính và điện thoại cùng WiFi
#
# 5. Backend phải đang chạy trước khi test app:
#    cd "Love Date Backend"
#    npm run start:dev
#
# 6. Test connection từ điện thoại:
#    Mở browser trên điện thoại
#    Truy cập: http://YOUR_IP:3000/api
#    Phải thấy JSON response (không phải error page)

# ====================
# LƯU Ý
# ====================
# - Firebase đã bị loại bỏ hoàn toàn khỏi app
# - App giờ chỉ cần Backend API để sync
# - App vẫn hoạt động offline 100% với SQLite
# - Không cần Firebase credentials nữa
# - File .env KHÔNG được commit vào Git (đã có trong .gitignore)
